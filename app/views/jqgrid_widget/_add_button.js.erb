<% if opts[:add_button] %>
      .navButtonAdd('#<%= opts[:pager_id] %>',{caption:'',title:'Add new record',buttonicon:'ui-icon-plus',
      	onClickButton:function(){
			<%= render :partial => 'jqgrid_widget/row_select', :locals => {:mode => :add, :opts => opts} %>
  //    		var gsr = jQuery("##{table}").getGridParam('selrow');
  //    		if(gsr){
  //    			jQuery("##{table}").GridToForm(gsr,"#order");
  //    		} else {
  //    			alert("Please select Row")
  //    		}
        } 
      });
<% end %>