<% if opts[:pager] %>
	pginput: true,
	pgbuttons: true,
	rowList:[#{opts[:pager][:rows_options] || ''}],
	rowNum:#{opts[:pager][:rows] || 20},
<% else %>
	pginput: false,
	pgbuttons: false,
	rowList:[],
<% end %>
